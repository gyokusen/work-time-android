# 作業時間管理 Android版 手引書

版：v1.2.1（2026-09-05）

PC版（Python＋WebView2）と同じ数えかたで、スマホだけで打刻・記録・集計ができます。
Androidのブラウザ（Chrome）で動く**単一ページのアプリ（PWA）**で、
ホーム画面に置くと、ふつうのアプリのように開けます。

---

## 1. ファイル

| ファイル | 何をするもの |
|---|---|
| `index.html` | 画面と中身。これ1枚でアプリになっている |
| `manifest.webmanifest` | 「ホーム画面に追加」したときの名前・アイコン・色 |
| `sw.js` | 電波が無くても動くようにする入れ物（Service Worker） |
| `icon/icon-192.png` ほか2枚 | アイコン |
| `初期マスタ.csv` | 最初に入れる作業・ターゲットの一覧（無くても動く） |

これらをまとめて置き場（GitHub Pages など）に上げます。
置き方は `GitHubPagesへの置き方.md` を見てください。

---

## 2. 入れかた（スマホ）

1. Chrome で置き場のURLを開く
2. 右上の「⋮」→ **［ホーム画面に追加］**（または［アプリをインストール］）
3. ホーム画面に時計のアイコンができる。以後はそこから開く

ホーム画面から開くと、アドレスバーの無い全画面になります。
一度開いたあとは、**電波が無くても動きます**（sw.js が控えを持つため）。

> 他の人に配るときは、この **URLを教えるだけ**です。
> ファイルを送ったり、インストーラを作ったりする必要はありません。

---

## 3. 画面

下に4つのタブがあります。

### 打刻
- 上の白いカードが**いまの作業**。作業名・ターゲット・経過時間・始めた時刻が出ます
- 下の作業ボタンを押すと、**いまの作業が終わって、その作業が始まります**（切替式）
- **同じ作業をもう一度押すと、確認が出ます。**
  「はい」ならそこで区切って、同じ作業をもう一度始めます（下の「区切って記録する」）。
  「いいえ」なら何もしません（押し間違いよけ）
- ［終了する］で、何もしていない状態に戻ります
- 進捗度と「ひとこと」を入れて［反映］を押すと、いまの作業に書き込みます

#### ひとこと・進捗は「いまの作業」のもの

作業ボタンや［終了する］を押すと、**画面のひとこと・進捗は、まず終わる区間に書き込まれます。**
そのあと新しい区間が、ひとこと空・進捗0 で始まります。
（いま何もしていないときだけ、ひとことはこれから始める作業に付きます）

#### 区切って記録する

読んだページ数のように、**同じ作業の中を細かく残したい**ときに使います。

1. 作業をしながら、ひとことに `p.10〜20` のように書く
2. **同じ作業のボタンをもう一度押す** → 確認が出るので「はい」
3. そこまでが1つの記録になり（ひとこと `p.10〜20` 付き）、続きが新しく始まる
4. 次は `p.21〜35` と書いて、また同じボタンを押す

記録タブで見ると、時間とページが区間ごとに並びます。
- ターゲット（案件・システム）は、打刻の前に選びます。**次の打刻にも引き継がれます**
- 選ぶ欄の右の［**＋ターゲット**］で、その場で登録できます。
  **保存すると、そのターゲットが選ばれた状態になる**ので、そのまま打刻できます
- 12時間を超えると、カードの色が変わります（打ち忘れの目印。設定で変えられます）

### 記録
- 日ごとの一覧です。◀ ▶ で日を移せます
- **行を押すと直せます**（開始・終了・作業・ターゲット・進捗度・メモ）
- ［＋記録］で、あとから足せます
- 右のチェックを付けて［選んだ行を消す］で消せます
- 記録と記録のあいだが5分以上あくと、上に**打ち忘れかも**と出ます

### 集計
- 期間を決めて［集計する］。今日・今週・今月のボタンもあります
- 作業別・ターゲット別・日別の3つが出ます
- 「休憩」の作業（席外し・バスルーム・昼休憩）は、作業時間から分けて数えます
- ［記録をCSVへ］［集計をCSVへ］で、CSVを端末にダウンロードします

### マスタ
- 打刻の画面に出す一覧です。行を押すと直せます
- 表は **ID／名称／区分／並び／使う**（ターゲットは区分なしの4列）。
  打刻画面に出る順は「並び」の小さい順です（初期値は 10, 20, 30…）。
  間に入れたいときは 15 のような値にします
- 使わなくなったものは「使わない」にすると、打刻の画面から消えます（**過去の記録は残ります**）
- ターゲットは、置き場の `初期マスタ.csv` に書いておけば最初から入ります

### ⚙（設定）
- **一日の始まり**（既定 05:00）… 深夜0時をまたいでも、この時刻より前は前の日の記録になります
- **打刻の丸め**（既定 0分）… 5 にすると5分きざみで打刻します
- **長すぎる印**（既定 12時間）
- **控えを書き出す（JSON）** / **控え・CSVを取り込む** /
  **初期マスタを読み込み直す** / **新しい版があるか確認する** / **記録を全部消す**

---

## 3.5 初期マスタ（作業・ターゲットの一覧）

置き場に `初期マスタ.csv` を置いておくと、**初めて開いたときにその中身が入ります**
（無ければアプリに内蔵の21件が入ります）。あとから差し替えたときは、
⚙→［**初期マスタを読み込み直す**］で反映できます。

CSVの形（1行目は見出し）：

```
種別,ID,名称,区分,並び,使う
作業,W001,席外し,休憩,10,1
作業,W010,コーディング,作業,100,1
ターゲット,T001,SMILe,,10,1
```

| 列 | 中身 |
|---|---|
| 種別 | `作業` または `ターゲット`。それ以外の行は読み飛ばす |
| ID | W001／T001 の形。**この値で同じものかを見分けます** |
| 名称 | 画面に出る名前 |
| 区分 | 作業のときだけ。`休憩` と書くと集計で作業時間から分ける。空なら「作業」 |
| 並び | 打刻画面に出る順。小さい順 |
| 使う | `0` なら「使わない」。空や `1` なら使う |

読み込みの決まり：

- **すでにあるIDは、名称・区分・並び・使うを書き換えます**
- **無いIDは足します**
- **CSVに書いていないものは消しません**（画面で足したものは残ります）
- **記録（打刻の中身）には一切触りません**
- 文字コードは自動で見分けます。PC版が出す cp932 のCSVもそのまま置けます

PC版の［集計］→［**マスタをCSVへ**］で、この形のCSVが出ます（PC版 v1.2.0 以降）。
出したファイルを `初期マスタ.csv` という名前で置き場に置いてください。

> **注意：置き場が GitHub Pages（public）なら、書いた名前はネットに公開されます。**
> 作業名称（コーディング・単体テストなど）は一般的な工程名なので問題ありませんが、
> **ターゲットに会社の案件名・システム名を書くと、それが公開されます。**
> 公開してよい名前だけを載せてください。

---

## 4. 記録の置き場所と、控え

記録は**この端末のブラウザの中**（IndexedDB）に入ります。サーバーには送りません。

そのため、次のときは記録が消えます。

- Chrome の「閲覧履歴データの削除」で**サイトデータ**を消したとき
- 端末を替えたとき、アプリを削除したとき

**ときどき⚙から［控えを書き出す］を押してください。**
JSONファイルが端末に落ちます。戻すときは［控え・CSVを取り込む］で、そのJSONを選びます
（取り込むと、いまの中身は**入れ替わります**）。

---

## 5. PC版との合流

Android版とPC版は、**同じ列・同じ並びのCSV**でやりとりします。

| 列 | 中身 |
|---|---|
| 開始日時 / 終了日時 | `2026-09-05 09:30:00` |
| 作業名称 / 作業ID | 打刻したときの名前と W001〜 |
| ターゲット名称 / ターゲットID | T001〜 |
| 作業時間 / 作業時間(分) / 進捗度 / メモ | |

### スマホ → PC
1. Android版の［集計］→ 期間を決める → ［記録をCSVへ］
2. 落ちたCSVをPCへ移す（メール・クラウド・USBなど）
3. PC版に取り込む

> **PC版にはまだCSVの取り込み口がありません。**
> PC版 v1.2.0 で［記録を取り込む］を足す予定です（別作業）。
> つまり **PC→スマホは今できて、スマホ→PCはこれから**です。

### PC → スマホ（**いま使えます**）
1. PC版の［記録をCSVへ］でCSVを作る
2. スマホに移す（メール添付・Googleドライブ・USBなど。スマホの「ダウンロード」に置く）
3. ⚙→［控え・CSVを取り込む］でそのCSVを選ぶ

- **文字コードは自動で見分けます。** PC版の cp932 も、UTF-8（BOM有り・無し）も、
  そのまま選べます。取り込んだあとの知らせに ［cp932］［UTF-8］ と出ます
- **開始日時＋作業ID＋作業名称が同じ行は読み飛ばします**（二重に入りません）。
  同じCSVを2回選んでも増えません
- CSVに出てくる作業・ターゲットで手元に無いものは、自動で一覧に足します
- PC側で**実行中だった行**（終了日時が空）が混じっていても、実行中は1つだけになります。
  **新しいほう**を残し、古いほうは「始まりの1分後」で閉じます

Android版が書き出すCSVは **UTF-8（BOM付き）**です。Excel でそのまま開けます。

---

## 6. できないこと（PC版との違い）

| | PC版 | Android版 |
|---|---|---|
| 保存先 | SQLite（work_time.db） | ブラウザの中（IndexedDB） |
| FAQタブ・問合せ窓口 | あり | **なし** |
| 「？」画面（この画面はどこのexeか等） | あり | **なし**（⚙の中に版だけ） |
| ログファイル | あり | なし |
| CSVの文字コード | cp932（読むのはまだ不可） | 書き出しは UTF-8（BOM付き）／読むのは cp932・UTF-8 の両方 |
| 記録の取り込み | まだ無い | あり（JSON・CSV） |

---

## 7. 直すとき

- 画面も中身も `index.html` の1枚だけです。直したら置き場に上げ直します
- **上げ直したあと、スマホ側は一度アプリを閉じて開き直す**と新しくなります。
  完全に終わらせるには、ホームに戻るだけでなく**タスク一覧からスワイプ**してください
- 新しくなったかは、画面の左上の版（`v1.1.2` など）で分かります
- アイコンや `manifest.webmanifest` を替えたときは、`sw.js` の `CACHE = "wta-v2"` を
  `"wta-v3"` のように**書き換えて**ください。古い控えが片づきます

### 新しい版のお知らせ（v1.2.1 から）

**アプリが自分で気づいて知らせます。** 使う人がキャッシュを気にする必要はありません。

- アプリを開いたとき、および他のアプリから戻ってきたときに、置き場の版を見に行きます
- 違っていたら、画面の下に「**新しい版 v1.2.2 があります（いまは v1.2.1）**」と出ます
- ［**更新する**］で読み込み直し、新しい版になります
- ［**あとで**］で消せます（その版は、次にアプリを開くまで出ません）
- 自分で確かめたいときは ⚙→［**新しい版があるか確認する**］

#### それでも古いままのとき

Androidのホーム画面アプリは、**閉じて開き直しても前の画面をそのまま出すことがあります**。
その場合は読み直しが起きないので、上のお知らせも出ません。次のどれかで確実になります。

1. **タスク一覧（履歴ボタン）からスワイプして消して**から、アイコンで開く
2. Chrome でそのURLを直接開く（アプリと同じ置き場です）
3. 一度ほかのアプリに切り替えてから戻る（そのときに見に行きます）

> **絶対にやらないこと**：Chrome の「Cookieとサイトデータ」の削除、
> アプリのアンインストール。**どちらも記録が全部消えます。**
> どうしても必要なときは、先に⚙→［控えを書き出す］でJSONを保存してください。

---

## 変更履歴

| 版 | 日付 | 変更内容 |
|---|---|---|
| 1.0.0 | 2026-09-05 | 初版。打刻・記録・集計・マスタ・設定、CSV入出力、JSON控え、PWA一式 |
| 1.0.1 | 2026-09-05 | 取り込みが cp932（PC版のCSV）も読めるように。文字コードを自動で見分ける。取り込みで実行中が2つにならないよう、古いほうを閉じる |
| 1.0.2 | 2026-09-05 | マスタの表に「並び」列を追加（作業・ターゲットとも）。PC版と同じ列立てにした |
| 1.1.0 | 2026-09-05 | 置き場の `初期マスタ.csv` を初回に読み込むようにした。⚙に［初期マスタを読み込み直す］を追加。PC版 v1.2.0 の［マスタをCSVへ］と同じ形 |
| 1.1.1 | 2026-09-05 | 打刻画面のターゲットの行を、下の作業の行と同じ形に（ラベルをやめ、選ぶ欄の右に［＋ターゲット］）。＋ターゲットで足したものが、そのまま選ばれるようにした |
| 1.1.2 | 2026-09-05 | 開き直しても新しくならない問題を直した。`sw.js` で画面のHTMLだけキャッシュを通さず取りに行く。入れ物を `wta-v2` に |
| 1.2.0 | 2026-09-05 | 同じ作業をもう一度押したとき、確認を出して「区切って始め直す」を選べるようにした。ひとこと・進捗は、区切るときに**終わる区間**へ書き込む（PC版の「切替の前に自動反映」と同じ規則に揃えた） |
| 1.2.1 | 2026-09-05 | 新しい版に自分で気づいて「更新する」を出すようにした。⚙に［新しい版があるか確認する］も追加 |
