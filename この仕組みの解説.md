# 作業時間管理 Android版 — この仕組みの解説

2026-09-05 作成

`https://gyokusen.github.io/work-time-android/` がどういう仕組みで動いているのか、
何が公開されていて何が公開されていないのかをまとめたものです。

---

## 1. ひとことで言うと

> **GitHub の置き場にファイルを置いておくと、そこを訪れた人のブラウザにファイルが届く。
> アプリはその人の端末の中で動き、記録もその人の端末の中にだけ残る。**

ご理解のとおりです。**ログインは要りません。**
そして、**訪れた人それぞれが、自分だけの台帳を持つアプリ**になります。

---

## 2. 全体の絵

```
  ┌──────────────── GitHub（インターネット上）────────────────┐
  │                                                          │
  │  リポジトリ  gyokusen/work-time-android  （public）      │
  │    index.html / sw.js / manifest / icon / 初期マスタ.csv │
  │                          │                               │
  │                    git push で更新                       │
  │                          ↓                               │
  │  GitHub Pages（ファイルをそのまま配るだけの置き場）      │
  │    https://gyokusen.github.io/work-time-android/         │
  │                                                          │
  └──────────────────────────┬───────────────────────────────┘
                             │ ①URLを開くと、ファイルが送られてくる
                             │   （送られるのは行きだけ。帰りは無い）
        ┌────────────────────┼────────────────────┐
        ↓                    ↓                    ↓
  ┌───────────┐        ┌───────────┐        ┌───────────┐
  │ 自分のスマホ│        │ 会社のPC  │        │ 他の人の端末│
  │           │        │           │        │           │
  │ ②ブラウザ │        │ ②ブラウザ │        │ ②ブラウザ │
  │  の中で   │        │  の中で   │        │  の中で   │
  │  アプリが │        │  アプリが │        │  アプリが │
  │  動く     │        │  動く     │        │  動く     │
  │           │        │           │        │           │
  │ ③記録は  │        │ ③記録は  │        │ ③記録は  │
  │  ここだけ │        │  ここだけ │        │  ここだけ │
  │ (IndexedDB)│        │ (IndexedDB)│        │ (IndexedDB)│
  └───────────┘        └───────────┘        └───────────┘
        ↑                    ↑                    ↑
        └──── 記録どうしは、まったくつながっていない ────┘
```

**大事なのは、矢印が「行き」しかないことです。**
GitHub からファイルが届くだけで、打刻した記録が GitHub に戻ることはありません。

---

## 3. 登場するもの

| もの | 役割 |
|---|---|
| **リポジトリ** | ファイルの保管庫。`git push` で新しくする |
| **GitHub Pages** | 保管庫の中身を、そのままインターネットに配る置き場 |
| **訪れた人のブラウザ** | 届いたファイルを読んで、**その端末の中で**アプリとして動かす |
| **IndexedDB** | ブラウザが端末の中に持っている入れ物。記録はここに入る |
| **Service Worker（sw.js）** | 届いたファイルの控えを端末に持つ係。電波が無くても動くのはこれのおかげ |

### GitHub Pages は「ただのファイル置き場」です

- HTML・JavaScript・画像・CSV を、**そのままの形で配る**だけ
- **サーバー側でプログラムは動きません**（PHPもデータベースもありません）
- だから「誰が何をしたか」を GitHub 側が記録することもありません
  （※アクセスした IP アドレスは、GitHub が保安目的で記録します）

---

## 4. なぜログインが要らないのか

ログインが必要になるのは、**サーバーにデータを預けるとき**です。
預けたデータを誰のものか見分けるために、名前と合言葉が要ります。

このアプリは**データを預けません**。記録は各自の端末の中だけです。
見分ける相手がいないので、**ログインの仕組み自体が要らない**わけです。

```
  よくあるWebサービス           このアプリ
  ─────────────────           ─────────────────
  ログイン → サーバーの         URLを開く → 端末の中の
  自分の場所を開く              自分の記録を開く
  （データはサーバー）           （データは端末）
```

---

## 5. 記録はどこにあるのか（いちばん大事なところ）

記録は **`https://gyokusen.github.io` に対して、その端末のそのブラウザが持っている入れ物**
（IndexedDB）に入ります。

### つまり、こういうことになります

| こうすると | 記録は |
|---|---|
| 同じスマホで、同じアプリを開き直す | **そのまま残る** |
| 同じスマホの Chrome でURLを開く | **同じ記録が見える**（アプリと同じ入れ物） |
| 自宅PCのブラウザで開く | **別の台帳**（空から始まる） |
| 会社PCのブラウザで開く | **別の台帳**（空から始まる） |
| 他の人が開く | **別の台帳**（空から始まる） |
| スマホの Chrome と Firefox で開く | **別の台帳**（ブラウザごとに別） |

**端末をまたいだ共有はできません。** それがこの作りの限界であり、同時に安心なところでもあります。

### 記録が消えるとき

- Chrome の「閲覧履歴データの削除」で**サイトデータ**を消したとき
- アプリをアンインストールしたとき
- 端末を替えたとき

**だから、ときどき⚙→［控えを書き出す］でJSONを保存してください。**
それが唯一の避難方法です。

### 覚えておくとよいこと（今後アプリを増やすとき）

入れ物は **`gyokusen.github.io` という単位**で分かれます。**URLの `/work-time-android/`
の部分では分かれません。** つまり、同じ `gyokusen.github.io` に別のアプリを置くと、
**入れ物を共有します**。

いまは中の名前（データベース名 `work_time`）が違えばぶつかりませんが、
今後アプリを増やすときは、**データベース名を必ずアプリごとに変えてください**。

---

## 6. 公開されているもの・されていないもの

リポジトリは **public（公開）** です。GitHub Free では、public でないと Pages が使えません。

### 公開されているもの（誰でも見られる）

- **アプリのソース**（`index.html` の中身すべて）
- **`初期マスタ.csv` の中身** … 作業名・ターゲット名がそのまま読めます
- 手引書などの `.md`
- コミットの履歴とメッセージ

URLを知らない人でも、GitHub で検索したり、検索エンジンに載ったりして
たどり着く可能性があります。**「URLを教えた人だけ」ではありません。**

### 公開されていないもの

- **打刻した記録**（開始・終了・ひとこと・進捗）
- ⚙の設定
- 誰がいつ使ったか

これらは端末の中にしかなく、**GitHub に送られることは一切ありません。**

> **ここが判断の分かれ目です。**
> 記録は安全ですが、**`初期マスタ.csv` に会社の案件名・システム名を書くと、それは公開されます。**
> 載せるのは、公開してよい名前だけにしてください。

---

## 7. 会社から開くときに考えること

技術的には、会社PCのブラウザで同じURLを開けば動きます（`github.io` はふつうのHTTPSサイトです）。
ただし次の3点は、技術ではなく**判断**の話です。

1. **会社のプロキシやフィルタが通すか**
   通らなければ開けません。これは会社の設定次第です

2. **会社PCのブラウザに記録が残る**
   自宅のスマホとは別の台帳になり、その中身は会社の端末に保存されます。
   自己啓発の記録を会社の端末に置いてよいか、という話になります

3. **業務の記録を扱うなら、別の判断が要る**
   いまは自己啓発用（英語・資格試験）なので問題は小さいですが、
   会社の作業内容を入れるなら、社外の置き場を経由することになるので、
   前に確認したとおり別の判断が必要です

---

## 8. PC版（exe を配る形）との比較

| | PC版（exe） | Android版（GitHub Pages） |
|---|---|---|
| 配り方 | インストーラ exe を渡す | **URLを教えるだけ** |
| 入れ替え | 全員が exe を実行し直す | **置き場を新しくするだけ**。各自は開くだけ |
| 動く場所 | Windows のみ | **ブラウザがあれば何でも**（スマホ・PC・タブレット） |
| 記録の置き場 | `work_time.db`（端末のファイル） | ブラウザの中（端末の中） |
| ログイン | 無し | 無し |
| ソースの公開 | 非公開 | **公開される** |
| ネットが要るか | 要らない | 初回だけ要る。以後はオフラインでも動く |
| 費用 | 無料 | 無料 |

**入れ替えの楽さが、いちばんの違いです。**
PC版は全員に配り直しますが、こちらは置き場を更新するだけで、次に開いた人から新しくなります。

---

## 9. 費用と制限（GitHub Pages）

無料です。ただし GitHub が定める上限があります。

| 項目 | 上限 |
|---|---|
| 元のリポジトリの大きさ | 1 GB（推奨） |
| 公開されるサイトの大きさ | 1 GB |
| 通信量 | 月 100 GB（ゆるやかな上限） |
| ビルド回数 | 1時間に10回（ゆるやかな上限） |

このアプリは全部で 100 KB ほどなので、どれも問題になりません。

**使ってはいけない用途**として、GitHub は次を挙げています。

> 商売のサイト、ネット通販、商用サービスの提供には使えません。
> パスワードやクレジットカード番号のような**大事な情報のやりとりにも使わないでください。**

このアプリはどれにも当たりません（何も送信しないので、そもそもやりとりがありません）。

---

## 10. この形が向くもの・向かないもの

### 向くもの

- 一人ひとりが自分の手元で使う道具（電卓・メモ・打刻・チェックリスト）
- 入れ替えを楽にしたいもの
- インストールを許してもらえない環境で使いたいもの

### 向かないもの

- **複数人で同じデータを見たい**もの（各自バラバラになります）
- **端末をまたいで同期したい**もの
- 人に見られたくないデータを扱うもの（サーバー側に置くならログインの仕組みが要ります）
- 消えては困るデータを、控えを取らずに置くこと

---

## 11. 言葉の対応表

| よく聞く言葉 | このアプリでは |
|---|---|
| 静的サイトホスティング | GitHub Pages。ファイルをそのまま配るだけの置き場 |
| クライアントサイド | 訪れた人の端末の中。このアプリは全部これ |
| サーバーサイド | 置き場の側。**このアプリでは何も動いていない** |
| PWA | ブラウザで動くのに、アプリのようにホーム画面に置ける作り |
| Service Worker | 控えを持つ係（`sw.js`）。オフラインで動くのはこれ |
| IndexedDB | ブラウザが端末に持つ入れ物。記録はここ |
| オリジン | `https://gyokusen.github.io`。入れ物が分かれる単位 |

---

## 12. 一行でまとめると

> **配るのはアプリだけ。記録は配らないし、集めもしない。**
> だからログインが要らず、そのかわり端末をまたげない。
